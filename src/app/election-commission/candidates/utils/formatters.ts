export const formatCandidateName = (name: string): string => {
  return name.trim().replace(/\s+/g, ' ');
};

export const formatPartyName = (party: string): string => {
  return party.trim().replace(/\s+/g, ' ');
};